fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic[email]>=2.0.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0
aiohttp>=3.9.0

# Auth — Clerk
clerk-backend-api>=5.0.0
bcrypt>=4.0.0

# Database — MongoDB + Beanie
motor>=3.7.0
beanie>=2.0.0

# Stripe
stripe>=9.0.0

# Redis / Background jobs
redis>=5.0.0
arq>=0.26.0

# Rate limiting
slowapi>=0.1.0


# Error tracking — Sentry
sentry-sdk[fastapi]>=2.20.0

# OpenTelemetry / APM (TASK-024)
opentelemetry-distro[otlp]>=0.25b0
opentelemetry-instrumentation-fastapi>=0.25b0
opentelemetry-instrumentation-httpx>=0.25b0
opentelemetry-instrumentation-pymongo>=0.25b0
opentelemetry-instrumentation-logging>=0.25b0

# Structured logging
loguru>=0.7.0

# TUI / CLI
textual>=0.80.0
pyotp==2.9.0

# Staging seed data
faker>=22.0.0

# PDF Generation (TASK-054)
weasyprint>=62.0.0

# Semantic caching / embeddings (TASK-057)
fastembed>=0.5.0,<1.0.0

# MCP Server (TASK-055)
mcp>=1.5.0,<2.0.0

# SSE support for MCP (optional — for Cursor/HTTP transport)
# uvicorn is already in dependencies above
starlette>=0.40.0

# Testing & Coverage
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
coverage>=7.0.0
